{
  "name": "guard-layer-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test:nokia": "node scripts/test-nokia.js"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "turbo": "latest"
  }
}
